<!DOCTYPE html>
<html lang="en" dir="ltr">
   <head>
      <meta charset="utf-8">
      <title>All Students</title>
   </head>
   <body>
      <h1>Students</h1>
      <p></p>
      <ul>
         <% @students.each do |student| %>
            <li><%= link_to student, student_path(student) %></li>

         <% end %>
      </ul>
   </body>
</html>
